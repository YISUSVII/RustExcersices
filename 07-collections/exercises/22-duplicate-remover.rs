fn unique_in_order(items: Vec<i32>) -> Vec<i32> {
    // TODO: keep first occurrence only
    let _ = items;
    unimplemented!("Implement unique_in_order")
}

fn main() {
    println!("{:?}", unique_in_order(vec![1, 2, 1, 3, 2]));
}
